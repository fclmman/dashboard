import { EmployeeWithShifts } from './employee-with-shifts';

describe('EmployeeWithShifts', () => {
  it('should create an instance', () => {
    expect(new EmployeeWithShifts()).toBeTruthy();
  });
});
